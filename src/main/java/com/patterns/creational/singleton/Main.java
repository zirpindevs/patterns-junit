package com.patterns.creational.singleton;

/**
 * Singleton
 * Patrón de diseño que se encarga de que una clase determinada
 * solamente pueda tener un único objeto, es decir, solo pueda instanciarse
 * una sola vez
 */
public class Main {

    private static final double MAX_PRICE = 50d;

    public static void main(String[] args) {

        double price = 10d;

        if(price < MAX_PRICE){

        }

        // MAX_PRICE = 50d;

    }
}
